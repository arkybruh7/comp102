#include<stdio.h>
#include<string.h>
int main()
{
    char bin[16];
    int len, i;
    gets(bin);
    len = strlen(bin);
    for(i=0;i<16;i++)
    {
        if(bin[i]!=0)
        {
            
        }
    }

}